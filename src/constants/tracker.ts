/**
 * SKM Protein Tracker — domain constants.
 * Single source of truth for all tracker magic values.
 * Re-exported from proteinTrackerService for backward compatibility.
 */

// ── Egg nutrition ─────────────────────────────────────────────
export const PROTEIN_PER_EGG  = 6;   // grams of protein per SKM egg
export const CALORIES_PER_EGG = 78;  // kcal per SKM egg

// ── Default goal ──────────────────────────────────────────────
export const DEFAULT_DAILY_GOAL = 60; // grams protein per day

// ── XP rewards ───────────────────────────────────────────────
export const XP_PER_EGG       = 10;
export const XP_PER_GOAL      = 25;
export const XP_PER_STREAK_DAY = 50;

// ── Coin rewards ──────────────────────────────────────────────
export const COINS_PER_EGG  = 5;
export const COINS_PER_GOAL = 20;
